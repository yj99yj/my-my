import React from 'react';
import Logo from '../images/Logo .svg'

const Nav = () => {
  return (
      <nav>
         <a href='/'>
          <img src={logo}/>
         </a>
      </nav>
  );    
}

export default Nav;